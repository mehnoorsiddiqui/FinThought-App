/**
 * Finicity APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  expandoObject,
  lazy,
  number,
  Schema,
  string,
} from '../schema';
import { Content, contentSchema } from './content';
import { Sort, sortSchema } from './sort';

export interface InvoiceBillingResponse {
  content: Content[];
  pageable: string;
  totalPages: number;
  totalElements: number;
  last: boolean;
  size: number;
  number: number;
  sort: Sort;
  numberOfElements: number;
  first: boolean;
  empty: boolean;
  [key: string]: unknown;
}

export const invoiceBillingResponseSchema: Schema<InvoiceBillingResponse> = expandoObject(
  {
    content: ['content', array(lazy(() => contentSchema))],
    pageable: ['pageable', string()],
    totalPages: ['totalPages', number()],
    totalElements: ['totalElements', number()],
    last: ['last', boolean()],
    size: ['size', number()],
    number: ['number', number()],
    sort: ['sort', lazy(() => sortSchema)],
    numberOfElements: ['numberOfElements', number()],
    first: ['first', boolean()],
    empty: ['empty', boolean()],
  }
);
