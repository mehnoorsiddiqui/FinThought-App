/**
 * Finicity APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { expandoObject, number, optional, Schema, string } from '../schema';

export interface PayrollEarningsRecord {
  /** The name of the earning as described by the payroll provider, if available. Note: Today the response provides the earnings summed into the defined earning types. In the future we may be able to provide the full earnings breakdown, at which point this field would be used to pass the earnings name as would be displayed on the employee's paystub. */
  name?: string;
  /** Earnings type for each earning. Types are: `base`, `overtime`, `bonus`, `other`, `commission` */
  type: string;
  /** Rate for each earning if available. */
  rate?: number;
  /** Earnings amount for each earning type */
  amount: number;
  /** Earnings YTD amount if available */
  amountYTD?: number;
  [key: string]: unknown;
}

export const payrollEarningsRecordSchema: Schema<PayrollEarningsRecord> = expandoObject(
  {
    name: ['name', optional(string())],
    type: ['type', string()],
    rate: ['rate', optional(number())],
    amount: ['amount', number()],
    amountYTD: ['amountYTD', optional(number())],
  }
);
