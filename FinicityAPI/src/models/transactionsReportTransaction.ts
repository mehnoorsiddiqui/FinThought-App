/**
 * Finicity APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { bigint, expandoObject, number, Schema, string } from '../schema';
import { CategoriesEnum, categoriesEnumSchema } from './categoriesEnum';

/** The fields used in the Transactions Report Transactions record */
export interface TransactionsReportTransaction {
  /** The Finicity ID of the financial transaction. */
  id: bigint;
  /** The total amount of the transaction. Transactions for deposits are positive values, and withdrawals and debits are negative values. */
  amount: number;
  /** A timestamp showing when the transaction was posted or cleared by the institution. */
  postedDate: bigint;
  /** The financial institution provides the description of the transaction (often known as the payee), but if it’s left blank, then Finicity passes the value: No description provided by institution. */
  description: string;
  /** A normalized payee, derived from the transaction's description and memo fields. */
  normalizedPayee: string;
  /** The unique identifier given by the financial institution for each transaction. */
  institutionTransactionId: string;
  /** A value from the Categories Enumerations, and assigned based on the payee name. */
  category: CategoriesEnum;
  /** The memo field of the transaction, as provided by the institution. The institution must provide either a description, a memo, or both. It is recommended to concatenate the two fields into a single value */
  memo: string;
  [key: string]: unknown;
}

export const transactionsReportTransactionSchema: Schema<TransactionsReportTransaction> = expandoObject(
  {
    id: ['id', bigint()],
    amount: ['amount', number()],
    postedDate: ['postedDate', bigint()],
    description: ['description', string()],
    normalizedPayee: ['normalizedPayee', string()],
    institutionTransactionId: ['institutionTransactionId', string()],
    category: ['category', categoriesEnumSchema],
    memo: ['memo', string()],
  }
);
