/**
 * Finicity APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  bigint,
  boolean,
  expandoObject,
  lazy,
  optional,
  Schema,
  string,
} from '../schema';
import { ErrorMessage, errorMessageSchema } from './errorMessage';
import {
  PayrollEmploymentHistory,
  payrollEmploymentHistorySchema,
} from './payrollEmploymentHistory';
import {
  PayrollReportRefreshConstraints,
  payrollReportRefreshConstraintsSchema,
} from './payrollReportRefreshConstraints';
import { TypeEnum, typeEnumSchema } from './typeEnum';

export interface VOIEPayrollReportRecord {
  /** Finicity's report ID */
  id: string;
  /** Finicity’s portfolio ID associated with the consumer on the report. */
  portfolioId: string;
  /** Finicity internal use only */
  gseEnabled: boolean;
  /** Customer type */
  customerType: string;
  /** Finicity ID for the customer */
  customerId: bigint;
  /** Unique requestId for this specific call request */
  requestId: string;
  /** Finicity report consumer ID (max length 32 characters) */
  consumerId: string;
  /** Last 4 digits of the report consumer’s Social Security number */
  consumerSsn: string;
  /** Name of Finicity partner requesting the report */
  requesterName: string;
  /** Report type */
  type: TypeEnum;
  /** inProgress, success, or failure */
  status: string;
  /** The date the report was generated */
  createdDate: bigint;
  /** Title of the report */
  title: string;
  constraints: PayrollReportRefreshConstraints;
  /** An array of employment histories, one for each of the consumer's verified employers. */
  employmentHistory: PayrollEmploymentHistory[];
  errors?: ErrorMessage[];
  [key: string]: unknown;
}

export const vOIEPayrollReportRecordSchema: Schema<VOIEPayrollReportRecord> = expandoObject(
  {
    id: ['id', string()],
    portfolioId: ['portfolioId', string()],
    gseEnabled: ['gseEnabled', boolean()],
    customerType: ['customerType', string()],
    customerId: ['customerId', bigint()],
    requestId: ['requestId', string()],
    consumerId: ['consumerId', string()],
    consumerSsn: ['consumerSsn', string()],
    requesterName: ['requesterName', string()],
    type: ['type', typeEnumSchema],
    status: ['status', string()],
    createdDate: ['createdDate', bigint()],
    title: ['title', string()],
    constraints: [
      'constraints',
      lazy(() => payrollReportRefreshConstraintsSchema),
    ],
    employmentHistory: [
      'employmentHistory',
      array(lazy(() => payrollEmploymentHistorySchema)),
    ],
    errors: ['errors', optional(array(lazy(() => errorMessageSchema)))],
  }
);
